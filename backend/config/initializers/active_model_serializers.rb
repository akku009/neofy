ActiveModelSerializers.config.adapter = :attributes

# Camelcase keys disabled — keep snake_case for consistency with Rails conventions.
# Change to :camel_case if your frontend prefers camelCase.
ActiveModelSerializers.config.key_transform = :unaltered
